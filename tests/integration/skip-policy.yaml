# Skip policy — every skip reason used by the bats fleet, classified.
#
# A skipped test reports as success, so the two cases must not be
# conflated:
#
#   env      the capability is genuinely absent from this machine or
#            lane (no Docker, no second VM, a kernel without the module
#            BTF a program needs). Skipping is the correct outcome and
#            `env_skip` always skips.
#
#   masking  a prerequisite the suite itself provides is missing, or an
#            earlier step produced nothing to assert on. Skipping keeps a
#            degraded dev box usable, but in the canonical lane it hides a
#            regression — so `soft_skip` FAILS under
#            EBPFSENTINEL_STRICT_SKIPS=1.
#
# Entries are matched as prefixes, so a reason ending in a shell
# interpolation (`... ${AGENT_BIN}`) is registered by its literal head.
# scripts/audit-skips.sh fails on any unregistered reason and on any bare
# `skip` call left in a suite.


env:
  - "TCP/22 already bound by another listener — the SSH threshold test needs the port to itself"
  - "ebpfsentinel:integration-test image not built — run the docker build first"
  - "minikube cluster could not start on this host"
  - "eth2 not present — NPTv6 needs the multi-NIC topology"
  - "capture interface"
  - "${tool} not available on attacker VM"
  - "${tool} not installed"
  - "${tool} not installed on agent VM"
  - "/proc/net/nf_conntrack unavailable — event stream not wired"
  - "Agent binary not found:"
  - "CFB HTTPS flood is sub-threshold for the volumetric rate-limiter; needs a dedicated TLS-rate policy"
  - "DLP alert captured but cgroup attribution unavailable on this kernel"
  - "Docker agent not healthy"
  - "Docker daemon not running"
  - "Docker engine not available"
  - "Docker not installed"
  - "Docker not running on agent VM"
  - "GeoIP mmdb not available ("
  - "JA4S needs a remote TLS server (ServerHello must transit the agent ingress); the harness TLS target is agent-local"
  - "Kernel BTF not available (/sys/kernel/btf/vmlinux missing)"
  - "Kernel BTF not available on agent VM"
  - "MHDDoS Tor disable flag missing ("
  - "MHDDoS not provisioned at"
  - "MHDDoS target"
  - "TCP/22 is in use by sshd in 2VM mode; suite 59 is single-netns only"
  - "TLS handshake flood is sub-threshold for the volumetric rate-limiter; needs a dedicated TLS-rate policy"
  - "Test requires 3-VM transit topology (set EBPF_3VM_MODE=true)"
  - "agent VM is missing the 'conntrack' tool (apt install conntrack)"
  - "agent binary not available on this host"
  - "agent binary not found:"
  - "agent kernel $kernel_str <"
  - "amplification script missing:"
  - "cannot SSH to agent VM at"
  - "capture engine not available (pcap-capture feature off?)"
  - "capture engine unavailable (pcap-capture feature off)"
  - "container resolver did not observe a miss ("
  - "curl not available on attacker VM"
  - "eBPF objects not found in"
  - "ebpfsentinel-agent CLI not installed on agent VM"
  - "ebpfsentinel-agent not found on agent VM"
  - "kubectl not installed"
  - "local openssl lacks X25519MLKEM768; PQ handshake assertion deferred"
  - "local openssl lacks X25519MLKEM768; PQ require-mode acceptance deferred"
  - "minikube not installed"
  - "no alert carried a container identity — degraded cgroup path"
  - "no eBPF environment (no local build, no Docker image '"
  - "no fou module BTF on this kernel"
  - "no nf_conntrack module BTF on this kernel"
  - "openssl not available on attacker VM"
  - "pktgen control files need root (passwordless sudo unavailable)"
  - "pktgen kernel module unavailable on this host"
  - "pktgen suite requires EBPF_2VM_MODE=true (real flood from attacker NIC)"
  - "pktgen target"
  - "pktgen thread"
  - "python3 not available on attacker VM"
  - "requires VLAN-capable veth (future)"
  - "requires root privileges"
  - "scapy not installed on attacker VM (run story 34.3 provisioner)"
  - "scapy not installed on attacker VM (run the attacker provisioner)"
  - "slowhttptest not installed (run story 34.3 provisioner)"
  - "slowhttptest target"
  - "slowloris is low-rate by design; not a volumetric rate-limit signal (slow-attack path covered by suite 39)"
  - "suite 38 requires EBPF_2VM_MODE=true (attacker VM driving real flood)"
  - "suite 39 requires EBPF_2VM_MODE=true (attacker VM driving real slow flood)"
  - "suite 40 requires EBPF_2VM_MODE=true (attacker VM crafting spoofed packets)"
  - "suite 41 requires EBPF_2VM_MODE=true (TLS target on agent VM)"
  - "suite 42 requires EBPF_2VM_MODE=true (forged-reply capture on attacker VM)"
  - "suite 43 requires EBPF_2VM_MODE=true (real+spoofed flood pair)"
  - "user namespaces not readable"
  - "v6 netns IDS wire probe is local-lane only (3-VM v6 path covered by the forward test)"
  - "warden binary not found after build"
  - "warden binary not found next to"

masking:
  - "warden log missing at"
  - "API not reachable at"
  - "API not reachable on agent VM"
  - "Agent failed to start"
  - "GeoIP lookup endpoint not available or mmdb does not contain 8.8.8.8"
  - "IOC count not reported in status endpoint"
  - "JA4 cache empty before restart (no clients fired earlier)"
  - "LB_MAGLEV map not available on agent VM"
  - "LB_MAGLEV map not available on agent VM (post-rebuild)"
  - "NAT service not enabled or NPTv6 endpoint missing"
  - "No pod found"
  - "STIX HTTP server not responding"
  - "TLS server not running"
  - "agent did not reload with the SSRF fixture"
  - "agent failed to come back up"
  - "agent pid not resolvable"
  - "alerts endpoint returned"
  - "alerts endpoint returned empty body"
  - "alerts endpoint returned nothing"
  - "backend DoT listener (:853) unreachable; dot-backend.service not running"
  - "backend iperf3 listener did not start"
  - "busybox container could not run"
  - "cannot read memory"
  - "cannot read memory after traffic"
  - "capture_on backend returned no pcap"
  - "capture_on failed on agent VM"
  - "control SYN never reached backend — transit inconclusive"
  - "could not resolve MAC for"
  - "could not start TLS target on agent VM at port"
  - "could not start ncat listener on agent VM at port"
  - "could not start tcpdump on attacker VM"
  - "curl to VIP failed (rc="
  - "iperf3 JSON output not parseable"
  - "iperf3 baseline failed"
  - "iperf3 did not produce output"
  - "iperf3 failed"
  - "launcher produced no output"
  - "metrics endpoint not available"
  - "neighbour TLS container could not run (image pull / apk / openssl)"
  - "no GeoIP-specific metrics found"
  - "no IPS rules available to patch"
  - "no IPv6 probe reached backend — transit link not established"
  - "no OTLP export recorded yet"
  - "no TLS client available to drive ServerHello"
  - "no TLS client available to drive malicious SNI"
  - "burst produced"
  - "no alert_id from previous test"
  - "no alerts emitted by this suite — MITRE assertion not applicable here"
  - "no alerts emitted during reload cycle — duplication check N/A"
  - "no alerts generated — cannot verify GeoIP enrichment"
  - "no alerts generated — traffic may not have matched a country-deny rule"
  - "no blacklist entries to check TTL on"
  - "no broker socket to probe"
  - "no delivery recorded yet"
  - "no ids-alert-test alert available"
  - "no mail recorded yet"
  - "no warden pid recorded — agent started without the broker"
  - "no zone-labelled metrics found"
  - "only"
  - "pcap empty (transit may not have reached backend)"
  - "pcap empty — transit path did not deliver the control packet"
  - "stop_capture failed on agent VM"
  - "tcpdump unavailable on agent VM"
  - "vip announcer never ran — no speaker transition in this topology"
  - "warden failed to build: $(cat /tmp/warden-build-$$.log)"
