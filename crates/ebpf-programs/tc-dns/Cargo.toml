[package]
name = "tc-dns"
version = "0.1.0"
edition = "2024"

[dependencies]
aya-ebpf = "0.1.1"
ebpf-common = { path = "../../ebpf-common", default-features = false }
network-types = "0.1.0"

[[bin]]
name = "tc-dns"
path = "src/main.rs"
