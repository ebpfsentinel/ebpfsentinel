// Primary ports are currently unused - application services are consumed
// directly by adapters as concrete types. If dependency inversion is needed
// later (e.g., for mock-based handler testing), define service traits here.
