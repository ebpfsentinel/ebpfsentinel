//! Adapters behind the heartbeat: the endpoint, the identifier file, and the
//! program status map the agent already keeps.

pub mod beacon_http;
pub mod installation_file;
pub mod program_inventory;
